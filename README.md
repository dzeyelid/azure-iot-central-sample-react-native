# azure-iot-central-sample-react-native
A sample of a device by React Native that connect to Azure IoT Central

## Create this environment

```bash
npm install -g expo-cli
expo init IoTCentralSample
cd IoTCentralSample
npm install
npm start
```